services:
  webui:
    build: ./src/webui
    ports:
      - "3000:80"  # Mapear el puerto 3000 del host al puerto 80 del contenedor
    environment:
      NODE_ENV: production

  # Otros servicios como microservicios de Python
  # ...